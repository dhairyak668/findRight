spring:
  datasource:
    url: jdbc:mysql://localhost:3306/findright
    username: root
    password: Anshu124$
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
          format_sql: true
      show-sql: true

server:
  error:
    include-message: always
    include-binding-errors: always